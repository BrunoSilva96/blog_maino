<!DOCTYPE html>
<html>
  <head>
    <title>BlogMaino</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Início", posts_path, :class => "nav-link active"%> 
        </li>
      </ul>
      <h4 class="me-auto">Blog Mainô</h4>
      <% if author_signed_in?%>
        <%= current_author.nickname %>
        <%= link_to 'Edit profile', edit_author_registration_path, :class => 'btn btn-outline-light m-2 p-1' %> 
        <%= link_to "Logout", destroy_author_session_path, method: :delete, :class => 'btn btn-outline-light p-1'  %> 
      <% else %>
        <%= link_to "Registrar", new_author_registration_path, :class => 'btn btn-outline-light m-2 p-1'  %> 
        <%= link_to "Fazer Login", new_author_session_path, :class => 'btn btn-outline-light p-1'  %>
      <% end %>
    </div>
  </div>
  </nav>
  <%= yield %>
  </body>
</html>

